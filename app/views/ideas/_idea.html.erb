	<li>

      <%= link_to idea.name, idea %>
        <% if current_user.admin? || current_user?(User.find(idea.user_id)) %>
   		 | <%= link_to "delete", idea, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>
	</li>