<% provide(:title, 'Teams') %>
<h1>Teams</h1>
<div class="text-right">
<%= link_to "Create Team", new_team_path, class: "btn btn-small btn-primary" %>
</div>

<div class="hero-unit">
<%= will_paginate %>

<ul class="users">
    <%= render @teams %>
</ul>

<%= will_paginate %>
</div>